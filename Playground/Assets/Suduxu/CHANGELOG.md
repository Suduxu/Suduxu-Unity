# Changelog

## [1.0.0] - 2026-04-23

- Added `package.json` so `Assets/Suduxu` can be consumed as a UPM package through Git URL + `?path=/Playground/Assets/Suduxu`.
- Added runtime assembly definition (`Scripts/Suduxu.Runtime.asmdef`).
- Removed JetBrains annotation dependency from config model classes to reduce package coupling.